<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.parkingadmin">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".BugReportCenter"></activity>
        <activity android:name=".FirebaseInstances">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity android:name=".SupportCenter" />
        <activity android:name=".DriverView" />
        <activity
            android:name=".DriverManagerView"
            android:label="@string/title_activity_driver_manager_view"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".NewDriver" />
        <activity android:name=".DriverManager" />
        <activity android:name=".MainMenu" />
        <activity android:name=".LoginActivity" />
    </application>

</manifest>